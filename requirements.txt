# Phase 1 dependencies for the edge computing allocation simulator.
# Versions are pinned with lower bounds to keep the environment reproducible
# while allowing minor upgrades.

numpy>=2.0
pandas>=2.2
pyyaml>=6.0
pytest>=8.0
